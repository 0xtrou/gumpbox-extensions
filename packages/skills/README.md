# @gumpbox/skills

Bundled starter skills for gumpbox MCP client plugins.

Each skill is markdown with YAML frontmatter (name, description, tags). Seeded into the user's gumpbox via `skills.create` on first plugin run.

See `src/*.md` for the source of truth. Edit there; never edit copies in `clients/<x>/skills/`.
