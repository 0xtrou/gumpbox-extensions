{
  "name": "@gumpbox/mcp-proxy",
  "version": "0.1.0",
  "description": "Standalone stdio MCP proxy for the gumpbox app. Bridges host editors to gumpbox's HTTP MCP server.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "gumpbox-mcp-proxy": "./bin/mcp-proxy.js"
  },
  "files": [
    "bin",
    "README.md"
  ],
  "scripts": {
    "build": "node esbuild.config.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo no-op",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gumpbox/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.16.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
